{#
  AVAILABLE VARIABLES:
  - sources: List[Dict] - The source code files that triggered this update.
      [{ "path": "src/foo.py", "content": "..." }, ...]
  - context_files: List[Dict] - Global context files defined in config.
      [{ "path": "README.md", "content": "..." }, ...]
  - doc_file: str - The path of the documentation file being updated (e.g., "docs/API.md").
  - doc_content: str - The current content of the documentation file (for reference).
#}
GLOBAL CONTEXT:
{% for ctx in context_files %}
-- {{ ctx.path }} --
{{ ctx.content }}
{% endfor %}

SOURCE CODE (Changed Files):
{% for src in sources %}
-- {{ src.path }} --
{{ src.content }}
{% endfor %}

CURRENT DOCUMENTATION ({{ doc_file }}):
{{ doc_content }}

INSTRUCTIONS:
Update the documentation above to reflect the changes in the source code.
Ensure all public APIs, classes, and functions are documented clearly.
Return only the Markdown content.
